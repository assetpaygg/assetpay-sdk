/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/auth/authenticate-client": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Authenticate a trading client
         * @description Creates a client JWT token for a Steam user. Requires a merchant API key with CORE_ACCESS scope. The returned token is used by the client for inventory, deposit, and withdrawal endpoints.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        clientSteamId: string;
                        clientTradeUrl: string;
                        clientData?: {
                            totalWager?: number;
                            /** Format: date-time */
                            registrationDate?: string | null;
                            kycHash?: string;
                            kycLevel?: number;
                            fiatDeposits?: boolean;
                            cryptoDeposits?: boolean;
                        };
                        clientId?: string;
                    };
                };
            };
            responses: {
                /** @description Client token issued */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                token: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/check-tradeurl": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Check trade URL validity
         * @description Validates a Steam trade URL and checks the user's trade eligibility (bans, escrow, privacy settings). Requires API key with CORE_ACCESS scope. Results are cached for 1 hour unless forceRefresh is true.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        tradeurl: string;
                        /** @default false */
                        forceRefresh?: boolean;
                    };
                };
            };
            responses: {
                /** @description Trade URL check result */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description 1=Normal, 2=CS2Banned, 3=RustBanned, 4=Banned, 5=InventoryPrivate, 6=TradeUrlInvalid, 7=UserNotFound, 8=UnknownError, 9=UserNotTradeable, 10=SteamUnavailable */
                                state: number;
                                /** @description Steam ID of the user */
                                steamid?: string;
                                /** @description Human-readable status message */
                                message: string;
                                /** @description Whether the user can trade */
                                canTrade?: boolean;
                                /** @description Trade restriction details */
                                reason?: {
                                    escrowDays: number;
                                    probation: boolean;
                                };
                                /** @description Steam ban record. A VAC ban blocks CS2 deposits (state 2, USER_NOT_TRADEABLE on deposit); game bans and Rust are unaffected */
                                bans?: {
                                    vacBanned: boolean;
                                    gameBans: number;
                                    /** @description Steam economy ban state: none, probation or banned */
                                    economyBan: string;
                                };
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/prices": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get item prices and offers
         * @description Returns the current price book for a given game. Each item carries a `deposit` price (what AssetPay pays for the item) and a `withdraw` price (fee-inclusive buy price you can pass straight back to /secure/buy). For CS2, `withdraw` is split into `standard` and `instant` delivery and each entry includes the catalog `itemId`; for Rust, `withdraw` is a single value. Doppler items are returned as a single entry with a `phases[]` breakdown, each phase carrying a deposit price and its own `withdraw.standard`/`withdraw.instant` (a phase often has a standard floor and no instant one, in which case `withdraw.instant` is null); the top-level `deposit`/`withdraw` carry the floor as a default. Withdraw prices already include your merchant fee. Requires API key with CORE_ACCESS scope.
         */
        get: {
            parameters: {
                query?: {
                    game?: number;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Per-item deposit and withdraw prices */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: ({
                                /** @description CS2 catalog item id, null when no withdraw floor */
                                itemId: string | null;
                                marketHashName: string;
                                /** @description Whether the item is accepted for deposit (deposit price > 0) */
                                accepted: boolean;
                                /** @description Deposit offer price in USD (what we pay for the item) */
                                deposit: number;
                                /** @description Fee-inclusive buy price per delivery mode; null when unavailable */
                                withdraw: {
                                    standard: number | null;
                                    instant: number | null;
                                };
                                /** @description Present for Doppler items: per-phase deposit + standard withdraw prices. The top-level deposit/withdraw carry the floor (cheapest phase / grouped floor) as a default. */
                                phases?: {
                                    /**
                                     * @description Doppler phase label
                                     * @enum {string}
                                     */
                                    phase: "Phase 1" | "Phase 2" | "Phase 3" | "Phase 4" | "Ruby" | "Sapphire" | "Black Pearl" | "Emerald";
                                    /** @description Whether this phase is accepted for deposit (deposit price > 0) */
                                    accepted: boolean;
                                    /** @description Deposit offer price in USD for this phase (what we pay) */
                                    deposit: number;
                                    /** @description Fee-inclusive buy price in USD for this phase, per delivery mode; either is null when unavailable. A phase often has a standard floor and no instant one. */
                                    withdraw: {
                                        standard: number | null;
                                        instant: number | null;
                                    };
                                }[];
                            } | {
                                marketHashName: string;
                                /** @description Whether the item is accepted for deposit (deposit price > 0) */
                                accepted: boolean;
                                /** @description Deposit offer price in USD (what we pay for the item) */
                                deposit: number;
                                /** @description Fee-inclusive buy price in USD; null when unavailable */
                                withdraw: number | null;
                            })[];
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/inventory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get Steam inventory (merchant self-trade)
         * @description Fetches a Steam account's inventory. The merchant passes a tradeUrl per request and AssetPay derives the steamId from it - no client authentication step is needed. Use this to discover items the merchant can sell to AssetPay via /secure/sell.
         */
        get: {
            parameters: {
                query: {
                    refresh?: boolean | ("true" | "false");
                    /** @description Return only items accepted for deposit (offer.accepted = true) */
                    acceptedOnly?: boolean | ("true" | "false");
                    game?: number;
                    search?: string;
                    sort?: string;
                    minPrice?: number | null;
                    maxPrice?: number | null;
                    page?: number;
                    limit?: number | null;
                    tradeUrl: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Inventory items + collateral */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                inventory: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                count: number;
                                /** Format: date-time */
                                updatedAt: string;
                                collateral: number;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Failed Dependency - Steam could not serve the request (inventory fetch failed, temporarily unavailable, or rate-limited upstream). The caller is not at fault; retry shortly. */
                424: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/sell": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sell skins to AssetPay (merchant self-trade)
         * @description Initiates a deposit-style trade where the merchant sends their own Steam items to AssetPay in exchange for USD wallet credit. Pricing must match the latest /secure/prices snapshot. Webhooks fire on state changes.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        items: {
                            itemId: string;
                            price: number;
                            /** @default 1 */
                            amount?: number;
                        }[];
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                        /** @default false */
                        isInstant?: boolean;
                        tradeUrl: string;
                    };
                };
            };
            responses: {
                /** @description Trade record */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                                /** @description Additional trades created when a basket is split into multiple trade offers. Each settles independently and emits its own webhooks. */
                                relatedTrades?: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "deposit" | "withdraw";
                                    /**
                                     * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                     * @enum {string}
                                     */
                                    source: "client" | "self";
                                    externalId?: string;
                                    externalClientUserId?: string;
                                    merchantId: string;
                                    clientUserId: string;
                                    clientTradeUrl: string;
                                    clientSteamID: string;
                                    /** @description Steam trade offer id. Present once the offer is sent. */
                                    offerID?: string;
                                    /**
                                     * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                     * @enum {string}
                                     */
                                    status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    game: string;
                                    items: {
                                        /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                        id: string;
                                        /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                        itemId?: string;
                                        /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                        externalId?: string;
                                        /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                        assetId?: string;
                                        appid: number;
                                        /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                        name?: string;
                                        marketHashName?: string;
                                        type?: string;
                                        iconUrl?: string;
                                        tradable: boolean;
                                        amount?: number;
                                        /**
                                         * @description Per-item status (withdrawals only)
                                         * @enum {string}
                                         */
                                        status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                        /**
                                         * @description Failure code for a failed withdraw item (withdrawals only)
                                         * @enum {string}
                                         */
                                        error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                        /** @description Market reference price in USD */
                                        marketPrice?: number;
                                        offer?: {
                                            /** @description Offer price in USD */
                                            price: number;
                                            /**
                                             * @description CS2 delivery mode
                                             * @enum {string}
                                             */
                                            delivery?: "standard" | "instant";
                                            maxAmount?: number;
                                            accepted?: boolean;
                                        };
                                        exterior?: string;
                                        rarity?: string;
                                        color?: string;
                                        wear?: string;
                                        paintSeed?: number;
                                        /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                        previewToken?: string;
                                        doppler?: {
                                            status?: number;
                                            name: string;
                                            paintIndex?: number;
                                        };
                                        fade?: {
                                            percentage: number;
                                        };
                                        hardened?: {
                                            status: number;
                                            name: string;
                                        };
                                        stickers?: {
                                            name: string;
                                            marketHashName?: string;
                                            slot: number;
                                            wear?: number;
                                            iconUrl: string;
                                        }[];
                                        charm?: {
                                            name: string;
                                            marketHashName?: string;
                                            iconUrl: string;
                                        };
                                        /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                        botInfo?: {
                                            name: string;
                                            avatar?: string;
                                            /** Format: date-time */
                                            joined?: string;
                                            steamId?: string;
                                        };
                                    }[];
                                    /** @description Total price in USD */
                                    totalPrice: number;
                                    /** @description Instant credit amount in USD */
                                    preCredit?: number;
                                    /** @description Amount held for hold period in USD */
                                    pendingCredit?: number;
                                    /** @description Whether instant crediting was requested */
                                    isInstant?: boolean;
                                    collateral?: {
                                        merchant: number;
                                        provider: number;
                                    };
                                    /** Format: date-time */
                                    holdEndDate?: string;
                                    /** @enum {string} */
                                    revertedBy?: "supplier" | "user";
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                    /**
                                     * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                    /**
                                     * Format: date-time
                                     * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                     */
                                    activeAt?: string;
                                    /**
                                     * Format: date-time
                                     * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                     */
                                    autoCancelAt?: string;
                                    /** Format: date-time */
                                    createdAt: string;
                                    /** Format: date-time */
                                    updatedAt: string;
                                }[];
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Failed Dependency - Steam could not serve the request (inventory fetch failed, temporarily unavailable, or rate-limited upstream). The caller is not at fault; retry shortly. */
                424: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/buy": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Buy skins from the marketplace (merchant self-trade)
         * @description Initiates a withdraw-style trade where AssetPay buys the requested listings from the upstream marketplace and delivers them to the supplied tradeUrl. The merchant wallet is debited. Optional `autoCancel` (minutes, 30-720, default 30; 720 disables) cancels still-undelivered items automatically after the period, with refunds settling via the trade webhook. CS2 only.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        items: {
                            itemId: string;
                            /** @description Price you quoted the end user. A lower current price is accepted and charged instead; a higher one is rejected unless it fits maxPrice or slippageBps. */
                            price: number;
                            /** @description Highest price you will pay for this item. Overrides slippageBps. Omit to reject any rise above price. */
                            maxPrice?: number;
                            /** @description Your own reference for this item. Cancel accepts it in place of the item id. */
                            externalId?: string;
                        }[];
                        /** @description Upward tolerance in basis points applied to every item lacking maxPrice (100 = 1%). Defaults to 0. */
                        slippageBps?: number;
                        /** @description Minutes after creation to automatically cancel still-undelivered items (30-720). Defaults to 30; 720 disables auto-cancel. CS2 only. */
                        autoCancel?: number;
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                        tradeUrl: string;
                    };
                };
            };
            responses: {
                /** @description Trade record */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/buy/quick": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Quick-buy skins from the marketplace (merchant self-trade)
         * @description Buys the N cheapest listings of a single item at or below a per-unit ceiling. Available for CS2 and Rust. Identify the item with either `itemId` or its exact `marketHashName` (itemId wins when both are sent); the per-unit `maxPrice` (fee-inclusive, gross) and `itemId` come from /secure/prices. `amount` is 1-200 and `delivery` is standard or instant. Optionally pin a Doppler `phase` (e.g. 'Ruby') to buy only that phase, priced against the phase floor for the requested delivery mode; phase and instant delivery are CS2-only. The trade starts empty and locks the worst-case total; the filled listings (possibly fewer than requested) and the refund of any unfilled / below-ceiling remainder settle asynchronously via the trade webhook. Optional `autoCancel` (minutes, 30-720, default 30; 720 disables) cancels still-undelivered items automatically after the period, with refunds settling via the trade webhook. CS2 only.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        /** @description Item id from /secure/prices (catalog id for CS2, listing id for Rust). Provide this OR marketHashName; when both are sent, itemId wins. */
                        itemId?: string;
                        /** @description Exact market hash name (e.g. 'AK-47 | Redline (Field-Tested)'), resolved to a catalog item id. Ignored when itemId is supplied. */
                        marketHashName?: string;
                        maxPrice: number;
                        amount: number;
                        /**
                         * @default standard
                         * @enum {string}
                         */
                        delivery?: "standard" | "instant";
                        /**
                         * @description Doppler phase to buy (e.g. 'Ruby'). Priced against the phase floor for the requested delivery mode.
                         * @enum {string}
                         */
                        phase?: "Phase 1" | "Phase 2" | "Phase 3" | "Phase 4" | "Ruby" | "Sapphire" | "Black Pearl" | "Emerald";
                        /** @description Minutes after creation to automatically cancel still-undelivered items (30-720). Defaults to 30; 720 disables auto-cancel. CS2 only. */
                        autoCancel?: number;
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                        /** @description Your own reference per requested unit, one per item, applied in order. Must hold exactly `amount` entries. Cancel accepts them in place of the item id. */
                        externalIds?: string[];
                        tradeUrl: string;
                    };
                };
            };
            responses: {
                /** @description Quick-buy trade initiated (items settle via webhook) */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/buy/{tradeId}/items/{itemId}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel an undelivered buy item (merchant self-trade)
         * @description Requests cancellation of a single undelivered item from a merchant self-trade buy. CS2 marketplace items only - Rust and internal-sourced items have no provider cancel and return TRADE_CANCEL_UNSUPPORTED. An item must be at least 30 minutes old and not yet terminal. On success the refund flows back via the trade webhook; the response only confirms the cancellation was accepted.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    /** @description Internal trade id or the externalId supplied at creation. */
                    tradeId: string;
                    /** @description The item's `id`, or the `externalId` you supplied for it at buy time. */
                    itemId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Cancellation accepted */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** Format: uuid */
                                tradeId: string;
                                /** Format: uuid */
                                itemId: string;
                                externalId?: string;
                                /** @enum {string} */
                                status: "cancelled";
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/buy/{tradeId}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel every undelivered item in a buy (merchant self-trade)
         * @description Requests cancellation of every still-cancellable item in the trade. CS2 marketplace items only - Rust and internal-sourced items have no provider cancel and return TRADE_CANCEL_UNSUPPORTED. An item must be at least 30 minutes old and not yet terminal. On success the refund flows back via the trade webhook; the response only confirms the cancellation was accepted. Best effort: a partial result is still a 200, with a per-item status and reason, so check the body rather than the status code.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    /** @description Internal trade id or the externalId supplied at creation. */
                    tradeId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Per-item cancellation outcomes */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** Format: uuid */
                                tradeId: string;
                                /** @description Items the provider considered cancellable */
                                requested: number;
                                cancelled: number;
                                failed: number;
                                items: {
                                    itemId: string;
                                    externalId?: string;
                                    /** @enum {string} */
                                    status: "cancelled" | "failed";
                                    /** @description Stable error key when the cancel was refused, e.g. TRADE_CANCEL_TOO_SOON or TRADE_NOT_CANCELLABLE. */
                                    reason?: string;
                                }[];
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/market": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Browse marketplace items (merchant self-trade)
         * @description Search and paginate the merchant-facing marketplace. Same listings the merchant's clients see via /client/market.
         */
        get: {
            parameters: {
                query?: {
                    game?: number;
                    search?: string;
                    sort?: "relevance" | "priceAsc" | "priceDesc" | "nameAsc" | "nameDesc";
                    minPrice?: number | null;
                    maxPrice?: number | null;
                    page?: number;
                    perPage?: number;
                    /** @description Sandbox flag. When true, every returned item is stamped with instant delivery, a placeholder preview token, and a synthetic holding bot so integrators can see the full item shape on cheap listings. */
                    development?: boolean | ("0" | "1" | "true" | "false");
                    source?: "internal" | "external" | "Xk9W";
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Marketplace search results */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                count: number;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/market/item": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get marketplace listings for one item (merchant self-trade)
         * @description Returns the available listings (price/wear/stickers/etc.) for a specific item type. Identify the item with either `itemId` or its exact `marketHashName` (itemId wins when both are sent). Optionally filter by Doppler `phase` (e.g. 'Ruby'), `delivery`, and a `floatMin`/`floatMax` wear range. Pass the resulting item ids to /secure/buy.
         */
        get: {
            parameters: {
                query?: {
                    /** @description CS2 catalog item id (from market suggestions or /secure/prices). Provide this OR marketHashName; when both are sent, itemId wins. */
                    itemId?: string;
                    /** @description Exact market hash name (e.g. 'AK-47 | Redline (Field-Tested)'), resolved to a catalog item id. Ignored when itemId is supplied. CS2 only. */
                    marketHashName?: string;
                    delivery?: "any" | "standard" | "instant";
                    /** @description Doppler phase to list (e.g. 'Ruby'). Returns only that phase's listings. CS2 only. */
                    phase?: "Phase 1" | "Phase 2" | "Phase 3" | "Phase 4" | "Ruby" | "Sapphire" | "Black Pearl" | "Emerald";
                    /** @description Lower bound on the item float/wear value (0-1). */
                    floatMin?: number | null;
                    /** @description Upper bound on the item float/wear value (0-1). */
                    floatMax?: number | null;
                    page?: number;
                    perPage?: number;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Available listings */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                id: string;
                                /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                itemId?: string;
                                /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                externalId?: string;
                                /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                assetId?: string;
                                appid: number;
                                /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                name?: string;
                                marketHashName?: string;
                                type?: string;
                                iconUrl?: string;
                                tradable: boolean;
                                amount?: number;
                                /**
                                 * @description Per-item status (withdrawals only)
                                 * @enum {string}
                                 */
                                status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                /**
                                 * @description Failure code for a failed withdraw item (withdrawals only)
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                /** @description Market reference price in USD */
                                marketPrice?: number;
                                offer?: {
                                    /** @description Offer price in USD */
                                    price: number;
                                    /**
                                     * @description CS2 delivery mode
                                     * @enum {string}
                                     */
                                    delivery?: "standard" | "instant";
                                    maxAmount?: number;
                                    accepted?: boolean;
                                };
                                exterior?: string;
                                rarity?: string;
                                color?: string;
                                wear?: string;
                                paintSeed?: number;
                                /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                previewToken?: string;
                                doppler?: {
                                    status?: number;
                                    name: string;
                                    paintIndex?: number;
                                };
                                fade?: {
                                    percentage: number;
                                };
                                hardened?: {
                                    status: number;
                                    name: string;
                                };
                                stickers?: {
                                    name: string;
                                    marketHashName?: string;
                                    slot: number;
                                    wear?: number;
                                    iconUrl: string;
                                }[];
                                charm?: {
                                    name: string;
                                    marketHashName?: string;
                                    iconUrl: string;
                                };
                                /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                            }[];
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/trades": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List merchant self-trades
         * @description Cursor-paginated history of trades initiated via /secure/buy and /secure/sell. End-user client trades are NOT included here - use the merchant module for combined history.
         */
        get: {
            parameters: {
                query?: {
                    cursor?: string;
                    limit?: number;
                    type?: "deposit" | "withdraw";
                    game?: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Paginated self-trade history */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                items: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "deposit" | "withdraw";
                                    /**
                                     * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                     * @enum {string}
                                     */
                                    source: "client" | "self";
                                    externalId?: string;
                                    externalClientUserId?: string;
                                    merchantId: string;
                                    clientUserId: string;
                                    clientTradeUrl: string;
                                    clientSteamID: string;
                                    /** @description Steam trade offer id. Present once the offer is sent. */
                                    offerID?: string;
                                    /**
                                     * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                     * @enum {string}
                                     */
                                    status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    game: string;
                                    items: {
                                        /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                        id: string;
                                        /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                        itemId?: string;
                                        /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                        externalId?: string;
                                        /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                        assetId?: string;
                                        appid: number;
                                        /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                        name?: string;
                                        marketHashName?: string;
                                        type?: string;
                                        iconUrl?: string;
                                        tradable: boolean;
                                        amount?: number;
                                        /**
                                         * @description Per-item status (withdrawals only)
                                         * @enum {string}
                                         */
                                        status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                        /**
                                         * @description Failure code for a failed withdraw item (withdrawals only)
                                         * @enum {string}
                                         */
                                        error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                        /** @description Market reference price in USD */
                                        marketPrice?: number;
                                        offer?: {
                                            /** @description Offer price in USD */
                                            price: number;
                                            /**
                                             * @description CS2 delivery mode
                                             * @enum {string}
                                             */
                                            delivery?: "standard" | "instant";
                                            maxAmount?: number;
                                            accepted?: boolean;
                                        };
                                        exterior?: string;
                                        rarity?: string;
                                        color?: string;
                                        wear?: string;
                                        paintSeed?: number;
                                        /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                        previewToken?: string;
                                        doppler?: {
                                            status?: number;
                                            name: string;
                                            paintIndex?: number;
                                        };
                                        fade?: {
                                            percentage: number;
                                        };
                                        hardened?: {
                                            status: number;
                                            name: string;
                                        };
                                        stickers?: {
                                            name: string;
                                            marketHashName?: string;
                                            slot: number;
                                            wear?: number;
                                            iconUrl: string;
                                        }[];
                                        charm?: {
                                            name: string;
                                            marketHashName?: string;
                                            iconUrl: string;
                                        };
                                        /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                        botInfo?: {
                                            name: string;
                                            avatar?: string;
                                            /** Format: date-time */
                                            joined?: string;
                                            steamId?: string;
                                        };
                                    }[];
                                    /** @description Total price in USD */
                                    totalPrice: number;
                                    /** @description Instant credit amount in USD */
                                    preCredit?: number;
                                    /** @description Amount held for hold period in USD */
                                    pendingCredit?: number;
                                    /** @description Whether instant crediting was requested */
                                    isInstant?: boolean;
                                    collateral?: {
                                        merchant: number;
                                        provider: number;
                                    };
                                    /** Format: date-time */
                                    holdEndDate?: string;
                                    /** @enum {string} */
                                    revertedBy?: "supplier" | "user";
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                    /**
                                     * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                    /**
                                     * Format: date-time
                                     * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                     */
                                    activeAt?: string;
                                    /**
                                     * Format: date-time
                                     * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                     */
                                    autoCancelAt?: string;
                                    /** Format: date-time */
                                    createdAt: string;
                                    /** Format: date-time */
                                    updatedAt: string;
                                }[];
                                nextCursor: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/trades/{tradeId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a trade by id or externalId
         * @description Returns the full record (status, items, collateral, bot info, error) for a trade. `tradeId` accepts either the internal trade id or the `externalId` you supplied at creation. Comma-separate up to 100 ids/externalIds (id1,id2,id3) to fetch several in one call — useful for polling multiple pending withdrawals. With one ref the response is a single trade (404 if not found); with multiple it is an array of the trades that resolved (refs that don't match are simply omitted, no 404). Scoped to the calling merchant: returns both your own /secure/* trades and trades belonging to your clients.
         */
        get: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    /** @description Internal trade id or your externalId. Comma-separate up to 100 (id1,id2,id3) to fetch multiple — the response is then an array. */
                    tradeId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description A single trade (one ref) or an array of trades (comma-separated refs) */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            } | {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            }[];
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/inventory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get client Steam inventory
         * @description Fetches the authenticated client's Steam inventory with pricing, search, filtering, and pagination. Inventory is cached for 15 minutes unless refresh=true. Includes collateral calculation.
         */
        get: {
            parameters: {
                query?: {
                    refresh?: boolean | ("true" | "false");
                    /** @description Return only items accepted for deposit (offer.accepted = true) */
                    acceptedOnly?: boolean | ("true" | "false");
                    game?: number;
                    search?: string;
                    sort?: string;
                    minPrice?: number | null;
                    maxPrice?: number | null;
                    page?: number;
                    limit?: number | null;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Client inventory with items, count, and collateral */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                inventory: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                count: number;
                                /** Format: date-time */
                                updatedAt: string;
                                /** @description Available collateral in USD */
                                collateral: number;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Failed Dependency - Steam could not serve the request (inventory fetch failed, temporarily unavailable, or rate-limited upstream). The caller is not at fault; retry shortly. */
                424: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trades": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get client trade history
         * @description Returns the authenticated client's trade history with pagination. Can be filtered by trade type (`deposit`/`withdraw`) and game.
         */
        get: {
            parameters: {
                query?: {
                    cursor?: string;
                    limit?: number;
                    type?: "deposit" | "withdraw";
                    game?: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Paginated trade history */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                items: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "deposit" | "withdraw";
                                    /**
                                     * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                     * @enum {string}
                                     */
                                    source: "client" | "self";
                                    externalId?: string;
                                    externalClientUserId?: string;
                                    merchantId: string;
                                    clientUserId: string;
                                    clientTradeUrl: string;
                                    clientSteamID: string;
                                    /** @description Steam trade offer id. Present once the offer is sent. */
                                    offerID?: string;
                                    /**
                                     * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                     * @enum {string}
                                     */
                                    status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    game: string;
                                    items: {
                                        /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                        id: string;
                                        /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                        itemId?: string;
                                        /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                        externalId?: string;
                                        /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                        assetId?: string;
                                        appid: number;
                                        /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                        name?: string;
                                        marketHashName?: string;
                                        type?: string;
                                        iconUrl?: string;
                                        tradable: boolean;
                                        amount?: number;
                                        /**
                                         * @description Per-item status (withdrawals only)
                                         * @enum {string}
                                         */
                                        status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                        /**
                                         * @description Failure code for a failed withdraw item (withdrawals only)
                                         * @enum {string}
                                         */
                                        error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                        /** @description Market reference price in USD */
                                        marketPrice?: number;
                                        offer?: {
                                            /** @description Offer price in USD */
                                            price: number;
                                            /**
                                             * @description CS2 delivery mode
                                             * @enum {string}
                                             */
                                            delivery?: "standard" | "instant";
                                            maxAmount?: number;
                                            accepted?: boolean;
                                        };
                                        exterior?: string;
                                        rarity?: string;
                                        color?: string;
                                        wear?: string;
                                        paintSeed?: number;
                                        /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                        previewToken?: string;
                                        doppler?: {
                                            status?: number;
                                            name: string;
                                            paintIndex?: number;
                                        };
                                        fade?: {
                                            percentage: number;
                                        };
                                        hardened?: {
                                            status: number;
                                            name: string;
                                        };
                                        stickers?: {
                                            name: string;
                                            marketHashName?: string;
                                            slot: number;
                                            wear?: number;
                                            iconUrl: string;
                                        }[];
                                        charm?: {
                                            name: string;
                                            marketHashName?: string;
                                            iconUrl: string;
                                        };
                                        /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                        botInfo?: {
                                            name: string;
                                            avatar?: string;
                                            /** Format: date-time */
                                            joined?: string;
                                            steamId?: string;
                                        };
                                    }[];
                                    /** @description Total price in USD */
                                    totalPrice: number;
                                    /** @description Instant credit amount in USD */
                                    preCredit?: number;
                                    /** @description Amount held for hold period in USD */
                                    pendingCredit?: number;
                                    /** @description Whether instant crediting was requested */
                                    isInstant?: boolean;
                                    collateral?: {
                                        merchant: number;
                                        provider: number;
                                    };
                                    /** Format: date-time */
                                    holdEndDate?: string;
                                    /** @enum {string} */
                                    revertedBy?: "supplier" | "user";
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                    /**
                                     * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                    /**
                                     * Format: date-time
                                     * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                     */
                                    activeAt?: string;
                                    /**
                                     * Format: date-time
                                     * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                     */
                                    autoCancelAt?: string;
                                    /** Format: date-time */
                                    createdAt: string;
                                    /** Format: date-time */
                                    updatedAt: string;
                                }[];
                                nextCursor: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/market/suggestions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Marketplace search suggestions
         * @description Returns search-as-you-type suggestions for marketplace items. CS2 only. Pass a returned id as itemId to /client/market/item to fetch listings.
         */
        get: {
            parameters: {
                query: {
                    q: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Search suggestions */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description Catalog item id, pass it as itemId to /client/market/item */
                                id: string;
                                marketHashName: string;
                                iconUrl: string;
                                rarity?: string;
                                rarityColor?: string;
                                itemType?: string;
                            }[];
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Failed Dependency - Steam could not serve the request (inventory fetch failed, temporarily unavailable, or rate-limited upstream). The caller is not at fault; retry shortly. */
                424: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/market": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Browse marketplace items
         * @description Search and paginate the merchant marketplace. CS2 returns a default listing slice; Rust returns the full Rust marketplace. Filterable by search term, sort, and price range.
         */
        get: {
            parameters: {
                query?: {
                    game?: number;
                    search?: string;
                    sort?: "relevance" | "priceAsc" | "priceDesc" | "nameAsc" | "nameDesc";
                    minPrice?: number | null;
                    maxPrice?: number | null;
                    page?: number;
                    perPage?: number;
                    /** @description Sandbox flag. When true, every returned item is stamped with instant delivery, a placeholder preview token, and a synthetic holding bot so integrators can see the full item shape on cheap listings. */
                    development?: boolean | ("0" | "1" | "true" | "false");
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Marketplace search results */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                count: number;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/market/item": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get marketplace listings for one item
         * @description Returns the available listings (price/wear/stickers/etc.) for a specific item type. CS2 only. Identify the item with either an `itemId` (from /client/market/suggestions or /client/market) or its exact `marketHashName` (itemId wins when both are sent). Optionally filter by Doppler `phase` (e.g. 'Ruby'), `delivery`, and a `floatMin`/`floatMax` wear range.
         */
        get: {
            parameters: {
                query?: {
                    /** @description CS2 catalog item id (from market suggestions or /secure/prices). Provide this OR marketHashName; when both are sent, itemId wins. */
                    itemId?: string;
                    /** @description Exact market hash name (e.g. 'AK-47 | Redline (Field-Tested)'), resolved to a catalog item id. Ignored when itemId is supplied. CS2 only. */
                    marketHashName?: string;
                    delivery?: "any" | "standard" | "instant";
                    /** @description Doppler phase to list (e.g. 'Ruby'). Returns only that phase's listings. CS2 only. */
                    phase?: "Phase 1" | "Phase 2" | "Phase 3" | "Phase 4" | "Ruby" | "Sapphire" | "Black Pearl" | "Emerald";
                    /** @description Lower bound on the item float/wear value (0-1). */
                    floatMin?: number | null;
                    /** @description Upper bound on the item float/wear value (0-1). */
                    floatMax?: number | null;
                    page?: number;
                    perPage?: number;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Available listings */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                id: string;
                                /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                itemId?: string;
                                /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                externalId?: string;
                                /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                assetId?: string;
                                appid: number;
                                /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                name?: string;
                                marketHashName?: string;
                                type?: string;
                                iconUrl?: string;
                                tradable: boolean;
                                amount?: number;
                                /**
                                 * @description Per-item status (withdrawals only)
                                 * @enum {string}
                                 */
                                status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                /**
                                 * @description Failure code for a failed withdraw item (withdrawals only)
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                /** @description Market reference price in USD */
                                marketPrice?: number;
                                offer?: {
                                    /** @description Offer price in USD */
                                    price: number;
                                    /**
                                     * @description CS2 delivery mode
                                     * @enum {string}
                                     */
                                    delivery?: "standard" | "instant";
                                    maxAmount?: number;
                                    accepted?: boolean;
                                };
                                exterior?: string;
                                rarity?: string;
                                color?: string;
                                wear?: string;
                                paintSeed?: number;
                                /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                previewToken?: string;
                                doppler?: {
                                    status?: number;
                                    name: string;
                                    paintIndex?: number;
                                };
                                fade?: {
                                    percentage: number;
                                };
                                hardened?: {
                                    status: number;
                                    name: string;
                                };
                                stickers?: {
                                    name: string;
                                    marketHashName?: string;
                                    slot: number;
                                    wear?: number;
                                    iconUrl: string;
                                }[];
                                charm?: {
                                    name: string;
                                    marketHashName?: string;
                                    iconUrl: string;
                                };
                                /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                            }[];
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/crypto/deposit-address": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the client's crypto deposit address
         * @description Returns the end user's permanent USDT/USDC deposit address on the chosen chain and creates it on the first call. The address never changes and can be reused for every deposit. On Solana each token has its own account, opened on request: pass `token` (default USDC, ignored on other chains) for the token the end user is about to send. The first call answers `pending` with no address, poll until it is `active` and `tokens` lists that token, and call again before each later deposit because an emptied account is closed. Returns ONCHAIN_ADDRESS_CAPACITY (503) when the merchant has too many Solana accounts open. Only a listed token on the listed network is credited, and a deposit under `minDepositCents` is not credited. A cleared deposit is credited to the merchant balance and announced with a `crypto_deposit` webhook carrying the `steamId`; crediting the end user is the merchant's side. Available to verified merchants.
         */
        get: {
            parameters: {
                query: {
                    chain: "ETH" | "BSC" | "SOL";
                    token?: "USDT" | "USDC";
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Deposit address */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                steamId: string;
                                /** @enum {string} */
                                chain: "ETH" | "BSC" | "SOL";
                                network: string;
                                testnet: boolean;
                                /** @enum {string} */
                                status: "active" | "pending" | "paused";
                                address: string | null;
                                tokenAccount: string | null;
                                tokens: {
                                    /** @enum {string} */
                                    token: "USDT" | "USDC";
                                    contract: string;
                                    decimals: number;
                                }[];
                                minDepositCents: number;
                                minConfirmations: number;
                                explorerUrl: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Service Unavailable - maintenance mode */
                503: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/crypto/deposits": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the client's crypto deposits
         * @description Lists the end user's native deposits, newest first. Pass `nextBefore` back as `before` for the next page.
         */
        get: {
            parameters: {
                query?: {
                    limit?: number;
                    before?: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Deposits page */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                deposits: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "crypto_deposit";
                                    /** @enum {string} */
                                    status: "pending" | "completed" | "failed" | "frozen" | "refunded" | "below_minimum";
                                    steamId: string;
                                    chain: string;
                                    token: string;
                                    amount: string;
                                    amountCents: number;
                                    cryptoAmount: string;
                                    address: string;
                                    txHash: string | null;
                                    from: string | null;
                                    createdAt: string;
                                    updatedAt: string;
                                    completedAt: string | null;
                                }[];
                                nextBefore: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/crypto/withdraw": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Withdraw crypto to the client's address
         * @description Sends USDT or USDC from the merchant balance to an address the end user chose. Because the request comes from the end user, the merchant's server is asked first: the withdrawal starts as `awaiting_approval` and a signed POST with `{ withdrawal }` goes to the merchant callback URL, the same approval call used for skin withdrawals. A 2xx answer approves it; a 4xx answer, or a body with `action: "reject"`, refuses it. No usable answer after three tries refuses it. The merchant must debit its own user before approving. `amountCents` is debited from the merchant balance and held at once; the end user receives `receiveCents`, which is the amount minus the flat network fee `feeCents` of the chain. `requestId` makes the call safe to retry: the same `requestId` for the same end user returns the first withdrawal and never creates a second one. Every later status change is announced with a `crypto_withdraw` webhook carrying the `steamId`; on `rejected`, `failed` and `cancelled` the full amount is back in the merchant balance. Returns ONCHAIN_CLIENT_WITHDRAW_LIMIT (403) with `details.scope` (`user` or `merchant`) and `details.remainingCents` when a daily limit is reached, ONCHAIN_CLIENT_WITHDRAW_DISABLED (503) while the feature is switched off, and INSUFFICIENT_BALANCE (400) when the merchant balance does not cover it. Large withdrawals are held for a manual review (`in_review`). Available to verified merchants.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        /** @enum {string} */
                        chain: "ETH" | "BSC" | "SOL";
                        /** @enum {string} */
                        token: "USDT" | "USDC";
                        address: string;
                        amountCents: number;
                        requestId: string;
                    };
                };
            };
            responses: {
                /** @description Withdrawal created */
                201: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "crypto_withdraw";
                                /** @enum {string} */
                                status: "awaiting_approval" | "in_review" | "approved" | "sent" | "completed" | "failed" | "rejected" | "cancelled";
                                steamId: string;
                                chain: string;
                                token: string;
                                amount: string;
                                amountCents: number;
                                feeCents: number;
                                receiveCents: number;
                                cryptoAmount: string;
                                address: string;
                                txHash: string | null;
                                reason: string | null;
                                createdAt: string;
                                updatedAt: string;
                                completedAt: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Conflict - the resource is in a state that forbids the request */
                409: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Service Unavailable - maintenance mode */
                503: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/crypto/withdrawals": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the client's crypto withdrawals
         * @description Lists the end user's crypto withdrawals, newest first. Pass `nextBefore` back as `before` for the next page.
         */
        get: {
            parameters: {
                query?: {
                    limit?: number;
                    before?: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Withdrawals page */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                withdrawals: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "crypto_withdraw";
                                    /** @enum {string} */
                                    status: "awaiting_approval" | "in_review" | "approved" | "sent" | "completed" | "failed" | "rejected" | "cancelled";
                                    steamId: string;
                                    chain: string;
                                    token: string;
                                    amount: string;
                                    amountCents: number;
                                    feeCents: number;
                                    receiveCents: number;
                                    cryptoAmount: string;
                                    address: string;
                                    txHash: string | null;
                                    reason: string | null;
                                    createdAt: string;
                                    updatedAt: string;
                                    completedAt: string | null;
                                }[];
                                nextBefore: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/crypto/withdraw": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Withdraw crypto to a client's address
         * @description Sends USDT or USDC from the merchant balance to an end user's address. The request comes from the merchant's own server, so no approval callback is made: debit your user first, then call this. `amountCents` is debited from the merchant balance and held at once; the end user receives `receiveCents`, which is the amount minus the flat network fee `feeCents` of the chain. `requestId` makes the call safe to retry: the same `requestId` for the same end user returns the first withdrawal and never creates a second one. Every later status change is announced with a `crypto_withdraw` webhook carrying the `steamId`; on `rejected`, `failed` and `cancelled` the full amount is back in the merchant balance. Returns ONCHAIN_CLIENT_WITHDRAW_LIMIT (403) with `details.scope` (`user` or `merchant`) and `details.remainingCents` when a daily limit is reached, ONCHAIN_CLIENT_WITHDRAW_DISABLED (503) while the feature is switched off, and INSUFFICIENT_BALANCE (400) when the merchant balance does not cover it. Large withdrawals are held for a manual review (`in_review`). Available to verified merchants. Requires CORE_ACCESS scope.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        /** @enum {string} */
                        chain: "ETH" | "BSC" | "SOL";
                        /** @enum {string} */
                        token: "USDT" | "USDC";
                        address: string;
                        amountCents: number;
                        requestId: string;
                        steamId: string;
                    };
                };
            };
            responses: {
                /** @description Withdrawal created */
                201: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "crypto_withdraw";
                                /** @enum {string} */
                                status: "awaiting_approval" | "in_review" | "approved" | "sent" | "completed" | "failed" | "rejected" | "cancelled";
                                steamId: string;
                                chain: string;
                                token: string;
                                amount: string;
                                amountCents: number;
                                feeCents: number;
                                receiveCents: number;
                                cryptoAmount: string;
                                address: string;
                                txHash: string | null;
                                reason: string | null;
                                createdAt: string;
                                updatedAt: string;
                                completedAt: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Conflict - the resource is in a state that forbids the request */
                409: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Service Unavailable - maintenance mode */
                503: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/crypto/withdrawals": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List a client's crypto withdrawals
         * @description Lists the end user's crypto withdrawals, newest first. Pass `nextBefore` back as `before` for the next page. Requires CORE_ACCESS scope.
         */
        get: {
            parameters: {
                query: {
                    limit?: number;
                    before?: string;
                    steamId: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Withdrawals page */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                withdrawals: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "crypto_withdraw";
                                    /** @enum {string} */
                                    status: "awaiting_approval" | "in_review" | "approved" | "sent" | "completed" | "failed" | "rejected" | "cancelled";
                                    steamId: string;
                                    chain: string;
                                    token: string;
                                    amount: string;
                                    amountCents: number;
                                    feeCents: number;
                                    receiveCents: number;
                                    cryptoAmount: string;
                                    address: string;
                                    txHash: string | null;
                                    reason: string | null;
                                    createdAt: string;
                                    updatedAt: string;
                                    completedAt: string | null;
                                }[];
                                nextBefore: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/crypto/deposit-address": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a client's crypto deposit address
         * @description Returns the end user's permanent USDT/USDC deposit address on the chosen chain and creates it on the first call. The address never changes and can be reused for every deposit. On Solana each token has its own account, opened on request: pass `token` (default USDC, ignored on other chains) for the token the end user is about to send. The first call answers `pending` with no address, poll until it is `active` and `tokens` lists that token, and call again before each later deposit because an emptied account is closed. Returns ONCHAIN_ADDRESS_CAPACITY (503) when the merchant has too many Solana accounts open. Only a listed token on the listed network is credited, and a deposit under `minDepositCents` is not credited. A cleared deposit is credited to the merchant balance and announced with a `crypto_deposit` webhook carrying the `steamId`; crediting the end user is the merchant's side. Available to verified merchants. Requires CORE_ACCESS scope.
         */
        get: {
            parameters: {
                query: {
                    chain: "ETH" | "BSC" | "SOL";
                    token?: "USDT" | "USDC";
                    steamId: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Deposit address */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                steamId: string;
                                /** @enum {string} */
                                chain: "ETH" | "BSC" | "SOL";
                                network: string;
                                testnet: boolean;
                                /** @enum {string} */
                                status: "active" | "pending" | "paused";
                                address: string | null;
                                tokenAccount: string | null;
                                tokens: {
                                    /** @enum {string} */
                                    token: "USDT" | "USDC";
                                    contract: string;
                                    decimals: number;
                                }[];
                                minDepositCents: number;
                                minConfirmations: number;
                                explorerUrl: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Service Unavailable - maintenance mode */
                503: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/secure/crypto/deposits": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List a client's crypto deposits
         * @description Lists the end user's native deposits, newest first. Pass `nextBefore` back as `before` for the next page. Requires CORE_ACCESS scope.
         */
        get: {
            parameters: {
                query: {
                    limit?: number;
                    before?: string;
                    steamId: string;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Deposits page */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                deposits: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "crypto_deposit";
                                    /** @enum {string} */
                                    status: "pending" | "completed" | "failed" | "frozen" | "refunded" | "below_minimum";
                                    steamId: string;
                                    chain: string;
                                    token: string;
                                    amount: string;
                                    amountCents: number;
                                    cryptoAmount: string;
                                    address: string;
                                    txHash: string | null;
                                    from: string | null;
                                    createdAt: string;
                                    updatedAt: string;
                                    completedAt: string | null;
                                }[];
                                nextBefore: string | null;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trading/deposit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Initiate a skin deposit
         * @description Initiates a deposit trade where the client sends skins to the platform. Each item must include the itemId from inventory and the agreed price (USD). Supports instant crediting (isInstant=true) where eligible; defaults to a full hold (isInstant=false). Uses distributed locks to prevent race conditions per Steam user.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        items: {
                            itemId: string;
                            price: number;
                            /** @default 1 */
                            amount?: number;
                        }[];
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                        /** @default false */
                        isInstant?: boolean;
                    };
                };
            };
            responses: {
                /** @description Deposit trade initiated */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                                /** @description Additional trades created when a basket is split into multiple trade offers. Each settles independently and emits its own webhooks. */
                                relatedTrades?: {
                                    id: string;
                                    /** @enum {string} */
                                    type: "deposit" | "withdraw";
                                    /**
                                     * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                     * @enum {string}
                                     */
                                    source: "client" | "self";
                                    externalId?: string;
                                    externalClientUserId?: string;
                                    merchantId: string;
                                    clientUserId: string;
                                    clientTradeUrl: string;
                                    clientSteamID: string;
                                    /** @description Steam trade offer id. Present once the offer is sent. */
                                    offerID?: string;
                                    /**
                                     * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                     * @enum {string}
                                     */
                                    status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    game: string;
                                    items: {
                                        /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                        id: string;
                                        /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                        itemId?: string;
                                        /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                        externalId?: string;
                                        /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                        assetId?: string;
                                        appid: number;
                                        /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                        name?: string;
                                        marketHashName?: string;
                                        type?: string;
                                        iconUrl?: string;
                                        tradable: boolean;
                                        amount?: number;
                                        /**
                                         * @description Per-item status (withdrawals only)
                                         * @enum {string}
                                         */
                                        status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                        /**
                                         * @description Failure code for a failed withdraw item (withdrawals only)
                                         * @enum {string}
                                         */
                                        error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                        /** @description Market reference price in USD */
                                        marketPrice?: number;
                                        offer?: {
                                            /** @description Offer price in USD */
                                            price: number;
                                            /**
                                             * @description CS2 delivery mode
                                             * @enum {string}
                                             */
                                            delivery?: "standard" | "instant";
                                            maxAmount?: number;
                                            accepted?: boolean;
                                        };
                                        exterior?: string;
                                        rarity?: string;
                                        color?: string;
                                        wear?: string;
                                        paintSeed?: number;
                                        /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                        previewToken?: string;
                                        doppler?: {
                                            status?: number;
                                            name: string;
                                            paintIndex?: number;
                                        };
                                        fade?: {
                                            percentage: number;
                                        };
                                        hardened?: {
                                            status: number;
                                            name: string;
                                        };
                                        stickers?: {
                                            name: string;
                                            marketHashName?: string;
                                            slot: number;
                                            wear?: number;
                                            iconUrl: string;
                                        }[];
                                        charm?: {
                                            name: string;
                                            marketHashName?: string;
                                            iconUrl: string;
                                        };
                                        /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                        botInfo?: {
                                            name: string;
                                            avatar?: string;
                                            /** Format: date-time */
                                            joined?: string;
                                            steamId?: string;
                                        };
                                    }[];
                                    /** @description Total price in USD */
                                    totalPrice: number;
                                    /** @description Instant credit amount in USD */
                                    preCredit?: number;
                                    /** @description Amount held for hold period in USD */
                                    pendingCredit?: number;
                                    /** @description Whether instant crediting was requested */
                                    isInstant?: boolean;
                                    collateral?: {
                                        merchant: number;
                                        provider: number;
                                    };
                                    /** Format: date-time */
                                    holdEndDate?: string;
                                    /** @enum {string} */
                                    revertedBy?: "supplier" | "user";
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                    /**
                                     * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                    /**
                                     * Format: date-time
                                     * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                     */
                                    activeAt?: string;
                                    /**
                                     * Format: date-time
                                     * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                     */
                                    autoCancelAt?: string;
                                    /** Format: date-time */
                                    createdAt: string;
                                    /** Format: date-time */
                                    updatedAt: string;
                                }[];
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Failed Dependency - Steam could not serve the request (inventory fetch failed, temporarily unavailable, or rate-limited upstream). The caller is not at fault; retry shortly. */
                424: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trading/withdraw": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Initiate a skin withdrawal
         * @description Initiates a withdrawal trade where the client receives skins from the platform. Each item must include the itemId from the prices endpoint and the agreed price (USD). Maximum 50 items per withdrawal. Price is validated against current offers. Optional `autoCancel` (minutes, 30-720, default 30; 720 disables) cancels still-undelivered items automatically after the period, with refunds settling via the trade webhook. CS2 only.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        items: {
                            itemId: string;
                            /** @description Price you quoted the end user. A lower current price is accepted and charged instead; a higher one is rejected unless it fits maxPrice or slippageBps. */
                            price: number;
                            /** @description Highest price you will pay for this item. Overrides slippageBps. Omit to reject any rise above price. */
                            maxPrice?: number;
                            /** @description Your own reference for this item. Cancel accepts it in place of the item id. */
                            externalId?: string;
                        }[];
                        /** @description Upward tolerance in basis points applied to every item lacking maxPrice (100 = 1%). Defaults to 0. */
                        slippageBps?: number;
                        /** @description Minutes after creation to automatically cancel still-undelivered items (30-720). Defaults to 30; 720 disables auto-cancel. CS2 only. */
                        autoCancel?: number;
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                    };
                };
            };
            responses: {
                /** @description Withdrawal trade initiated */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trading/withdraw/quick": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Initiate a quick skin withdrawal
         * @description Buys the N cheapest listings of a single item at or below a per-unit ceiling, instead of naming specific listings. Available for CS2 and Rust. Identify the item with either `itemId` or its exact `marketHashName` (itemId wins when both are sent); the per-unit `maxPrice` (fee-inclusive, gross) and `itemId` come from /secure/prices. `amount` is the quantity (1-200) and `delivery` is standard or instant. Optionally pin a Doppler `phase` (e.g. 'Ruby') to buy only that phase, priced against the phase floor for the requested delivery mode; phase and instant delivery are CS2-only. The trade starts empty and locks the worst-case total; the filled listings (which may be fewer than requested) and the refund of any unfilled / below-ceiling remainder settle asynchronously via the trade webhook. Optional `autoCancel` (minutes, 30-720, default 30; 720 disables) cancels still-undelivered items automatically after the period, with refunds settling via the trade webhook. CS2 only.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        /** @description Item id from /secure/prices (catalog id for CS2, listing id for Rust). Provide this OR marketHashName; when both are sent, itemId wins. */
                        itemId?: string;
                        /** @description Exact market hash name (e.g. 'AK-47 | Redline (Field-Tested)'), resolved to a catalog item id. Ignored when itemId is supplied. */
                        marketHashName?: string;
                        maxPrice: number;
                        amount: number;
                        /**
                         * @default standard
                         * @enum {string}
                         */
                        delivery?: "standard" | "instant";
                        /**
                         * @description Doppler phase to buy (e.g. 'Ruby'). Priced against the phase floor for the requested delivery mode.
                         * @enum {string}
                         */
                        phase?: "Phase 1" | "Phase 2" | "Phase 3" | "Phase 4" | "Ruby" | "Sapphire" | "Black Pearl" | "Emerald";
                        /** @description Minutes after creation to automatically cancel still-undelivered items (30-720). Defaults to 30; 720 disables auto-cancel. CS2 only. */
                        autoCancel?: number;
                        /** @default 730 */
                        game?: string;
                        externalId?: string;
                        /** @description Your own reference per requested unit, one per item, applied in order. Must hold exactly `amount` entries. Cancel accepts them in place of the item id. */
                        externalIds?: string[];
                    };
                };
            };
            responses: {
                /** @description Quick withdrawal trade initiated (items settle via webhook) */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                /** @enum {string} */
                                type: "deposit" | "withdraw";
                                /**
                                 * @description Trade origin: "client" (end-user flow) or "self" (merchant self-trade)
                                 * @enum {string}
                                 */
                                source: "client" | "self";
                                externalId?: string;
                                externalClientUserId?: string;
                                merchantId: string;
                                clientUserId: string;
                                clientTradeUrl: string;
                                clientSteamID: string;
                                /** @description Steam trade offer id. Present once the offer is sent. */
                                offerID?: string;
                                /**
                                 * @description Trade status. In-flight: initiated, pending, active, hold. Terminal: completed, failed, canceled, declined, reverted. Deposit flow: initiated → pending (offer created, awaiting bot confirmation) → active (offer confirmed, offerID present) → hold → completed. Withdraw flow: initiated → pending → active → hold → completed.
                                 * @enum {string}
                                 */
                                status: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                game: string;
                                items: {
                                    /** @description On CS2 withdraw items this is the listing id you quoted, unchanged for the item's whole life (the delivered assetid arrives under assetId); cancel endpoints accept it alongside your externalId. On inventory and deposit items it is the encoded item id. */
                                    id: string;
                                    /** @description The parent catalog item this listing belongs to (hex, e.g. "0010f8d48278ccb8") — the same id you pass to `/market/item`, while `id` above identifies the individual listing. Present on CS2 `GET /market` rows only: `/market/item` omits it because you named the item in the request, and it is never present on Rust, inventory, deposit or withdraw items. */
                                    itemId?: string;
                                    /** @description Your own per-item reference, when you supplied one at buy time. Cancel accepts it in place of id. Omitted on stacked Rust rows, which stand for several items at once. */
                                    externalId?: string;
                                    /** @description Raw Steam assetid. Always present on inventory listings and deposit trade items. Omitted on withdraw items until the item lands in the recipient's Steam inventory. */
                                    assetId?: string;
                                    appid: number;
                                    /** @description Omitted on bare withdraw items where AssetPay has no Steam-side metadata yet */
                                    name?: string;
                                    marketHashName?: string;
                                    type?: string;
                                    iconUrl?: string;
                                    tradable: boolean;
                                    amount?: number;
                                    /**
                                     * @description Per-item status (withdrawals only)
                                     * @enum {string}
                                     */
                                    status?: "initiated" | "pending" | "active" | "hold" | "completed" | "failed" | "canceled" | "declined" | "reverted";
                                    /**
                                     * @description Failure code for a failed withdraw item (withdrawals only)
                                     * @enum {string}
                                     */
                                    error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED";
                                    /** @description Market reference price in USD */
                                    marketPrice?: number;
                                    offer?: {
                                        /** @description Offer price in USD */
                                        price: number;
                                        /**
                                         * @description CS2 delivery mode
                                         * @enum {string}
                                         */
                                        delivery?: "standard" | "instant";
                                        maxAmount?: number;
                                        accepted?: boolean;
                                    };
                                    exterior?: string;
                                    rarity?: string;
                                    color?: string;
                                    wear?: string;
                                    paintSeed?: number;
                                    /** @description Steam inspect/preview token for rendering the item's in-game preview. */
                                    previewToken?: string;
                                    doppler?: {
                                        status?: number;
                                        name: string;
                                        paintIndex?: number;
                                    };
                                    fade?: {
                                        percentage: number;
                                    };
                                    hardened?: {
                                        status: number;
                                        name: string;
                                    };
                                    stickers?: {
                                        name: string;
                                        marketHashName?: string;
                                        slot: number;
                                        wear?: number;
                                        iconUrl: string;
                                    }[];
                                    charm?: {
                                        name: string;
                                        marketHashName?: string;
                                        iconUrl: string;
                                    };
                                    /** @description Holding bot identity. Present on internal-pool marketplace items. */
                                    botInfo?: {
                                        name: string;
                                        avatar?: string;
                                        /** Format: date-time */
                                        joined?: string;
                                        steamId?: string;
                                    };
                                }[];
                                /** @description Total price in USD */
                                totalPrice: number;
                                /** @description Instant credit amount in USD */
                                preCredit?: number;
                                /** @description Amount held for hold period in USD */
                                pendingCredit?: number;
                                /** @description Whether instant crediting was requested */
                                isInstant?: boolean;
                                collateral?: {
                                    merchant: number;
                                    provider: number;
                                };
                                /** Format: date-time */
                                holdEndDate?: string;
                                /** @enum {string} */
                                revertedBy?: "supplier" | "user";
                                botInfo?: {
                                    name: string;
                                    avatar?: string;
                                    /** Format: date-time */
                                    joined?: string;
                                    steamId?: string;
                                };
                                /**
                                 * @description Trade-level failure reason, omitted when there is none. Withdraw: set only when the whole trade failed with one shared cause.
                                 * @enum {string}
                                 */
                                error?: "LISTING_UNAVAILABLE" | "NO_LISTING_AT_PRICE" | "PRICE_CHANGED" | "TRADE_URL_INVALID" | "STEAM_ACCOUNT_RESTRICTED" | "BUYER_TRADE_RESTRICTED" | "OFFER_NOT_ACCEPTED" | "MARKET_UNAVAILABLE" | "PURCHASE_FAILED" | "USER_TRADE_RESTRICTED" | "USER_INVENTORY_PRIVATE" | "USER_ESCROW" | "USER_NOT_FOUND" | "SERVICE_UNAVAILABLE" | "MERCHANT_REJECTED" | "APPROVAL_TIMEOUT";
                                /**
                                 * Format: date-time
                                 * @description When the trade first became active (the trade offer went live at the user). Absent until then.
                                 */
                                activeAt?: string;
                                /**
                                 * Format: date-time
                                 * @description When items the seller has not yet sent will be canceled automatically. Items already offered to the user or in Steam hold are never auto-canceled. Absent when auto-cancel is disabled or for Rust withdrawals.
                                 */
                                autoCancelAt?: string;
                                /** Format: date-time */
                                createdAt: string;
                                /** Format: date-time */
                                updatedAt: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trading/withdraw/{tradeId}/items/{itemId}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel an undelivered withdrawal item
         * @description Requests cancellation of a single undelivered withdrawal item. CS2 marketplace items only - Rust and internal-sourced items have no provider cancel and return TRADE_CANCEL_UNSUPPORTED. An item must be at least 30 minutes old and not yet terminal. On success the refund flows back via the trade webhook; the response only confirms the cancellation was accepted.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    /** @description Internal trade id or the externalId supplied at creation. */
                    tradeId: string;
                    /** @description The item's `id`, or the `externalId` you supplied for it at buy time. */
                    itemId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Cancellation accepted */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** Format: uuid */
                                tradeId: string;
                                /** Format: uuid */
                                itemId: string;
                                externalId?: string;
                                /** @enum {string} */
                                status: "cancelled";
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/trading/withdraw/{tradeId}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel every undelivered item in a withdrawal
         * @description Requests cancellation of every still-cancellable item in the trade. CS2 marketplace items only - Rust and internal-sourced items have no provider cancel and return TRADE_CANCEL_UNSUPPORTED. An item must be at least 30 minutes old and not yet terminal. On success the refund flows back via the trade webhook; the response only confirms the cancellation was accepted. Best effort: a partial result is still a 200, with a per-item status and reason, so check the body rather than the status code.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    /** @description Internal trade id or the externalId supplied at creation. */
                    tradeId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Per-item cancellation outcomes */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** Format: uuid */
                                tradeId: string;
                                /** @description Items the provider considered cancellable */
                                requested: number;
                                cancelled: number;
                                failed: number;
                                items: {
                                    itemId: string;
                                    externalId?: string;
                                    /** @enum {string} */
                                    status: "cancelled" | "failed";
                                    /** @description Stable error key when the cancel was refused, e.g. TRADE_CANCEL_TOO_SOON or TRADE_NOT_CANCELLABLE. */
                                    reason?: string;
                                }[];
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/verification/start": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Start identity verification
         * @description Initiates a Signicat PictureID verification (document scan + selfie). Returns a `captureUrl` the client should open in a browser or webview to complete the capture. An optional `email` can be provided which is stored on the client record for future identity linking.
         *
         *     **Limits:**
         *     - Returns `VERIFICATION_ALREADY_VERIFIED` if the account is already verified (level >= 1).
         *     - Returns `VERIFICATION_IN_PROGRESS` if an active process was created within the last 30 minutes.
         *     - Returns `VERIFICATION_NOT_AVAILABLE` if Signicat credentials are not configured.
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: {
                content: {
                    "application/json": {
                        /** Format: email */
                        email?: string;
                    };
                };
            };
            responses: {
                /** @description Verification started  open `captureUrl` in the client. */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description URL to redirect/open for document + selfie capture */
                                captureUrl: string;
                                /** @description Signicat dossier ID */
                                dossierId: string;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Service Unavailable - maintenance mode */
                503: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/verification/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get verification status
         * @description Returns the current verification level of the authenticated client, their associated email, the most recent Signicat process (if any), and the linked verified identity (if any).
         */
        get: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Current verification status */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description Current verification level (0 = none, 1 = picture_id) */
                                verificationLevel: number;
                                /**
                                 * Format: email
                                 * @description Email address associated with this account
                                 */
                                email: string | null;
                                /** @description Most recent verification attempt, or null if none */
                                latestProcess: {
                                    /** @description Signicat dossier ID */
                                    dossierId: string;
                                    /** @description Signicat process ID */
                                    processId: string | null;
                                    /** @description URL the client should open to complete document capture */
                                    captureUrl: string;
                                    /** @enum {string} */
                                    status: "CREATED" | "CAPTURE_PENDING" | "PROCESSING" | "ACCEPTED" | "REJECTED" | "ERROR" | "EXPIRED";
                                    /** Format: date-time */
                                    createdAt: string;
                                    /** Format: date-time */
                                    completedAt: string | null;
                                } | null;
                                /** @description Verified identity linked to this account, or null if not yet verified */
                                linkedIdentity: {
                                    /**
                                     * Format: uuid
                                     * @description Verified identity ID
                                     */
                                    id: string;
                                    firstName: string | null;
                                    lastName: string | null;
                                    /** @description ISO 3166-1 alpha-3 country code of the document */
                                    documentCountry: string | null;
                                    /** Format: date-time */
                                    linkedAt: string;
                                } | null;
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/client/verification/link": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Link account to existing identity
         * @description Links the authenticated client's account to an existing verified identity via email match. Use this after a successful verification completes for another account with the same email.
         *
         *     No request body is needed  the identity is looked up automatically by the client's email address (set during the `/start` call).
         *
         *     **Requirements:**
         *     - The client must have an email set on their account (provided during `/start`).
         *     - A verified identity with a matching email must exist (case-insensitive).
         *     - The identity must not be banned.
         *     - The identity must not have reached its maximum linked account limit.
         *
         *     On success, the account's `verificationLevel` is immediately upgraded to `1` (picture_id).
         */
        post: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Account successfully linked to the verified identity */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @enum {boolean} */
                                linked: true;
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/wallets/me/balance": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get merchant wallet balance
         * @description Returns the authenticated merchant's wallet in USD. balance is spendable now; pendingBalance holds uncleared deposits and funds locked for in-flight withdrawals; escrowBalance holds skin deposits still in their hold period.
         */
        get: {
            parameters: {
                query?: never;
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Merchant balances */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                /** @description Available balance in USD */
                                balance: number;
                                /** @description Pending (uncleared) balance in USD */
                                pendingBalance: number;
                                /** @description Escrowed balance held for in-flight trades, in USD */
                                escrowBalance: number;
                                currency: string;
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/ledger/transactions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List ledger transactions
         * @description Wallet funding history for the authenticated merchant, newest first: deposits, withdrawals, refunds and internal transfers, plus admin credits and debits (type ADMIN_CREDIT / ADMIN_DEBIT). Trade settlements are not included. INITIATED fundings older than 24 hours are hidden. Filter by type, status and a created-at date range.
         */
        get: {
            parameters: {
                query?: {
                    type?: "DEPOSIT" | "WITHDRAWAL" | "REFUND" | "INTERNAL_TRANSFER";
                    status?: "INITIATED" | "PENDING" | "PENDING_ADMIN" | "APPROVED" | "REJECTED" | "COMPLETED" | "FAILED" | "CANCELLED" | "REFUNDED" | "EXPIRED";
                    /** @description Created at or after (ISO 8601) */
                    startDate?: string | null;
                    /** @description Created at or before (ISO 8601) */
                    endDate?: string | null;
                    limit?: number;
                    offset?: number | null;
                };
                header?: never;
                path?: never;
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Paginated transaction history */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                items: {
                                    id: string;
                                    type: string;
                                    status: string;
                                    amount: {
                                        usd: number;
                                        crypto: string;
                                        token: string | null;
                                        chain: string | null;
                                    };
                                    fee: number;
                                    address: string | null;
                                    txnHash: string | null;
                                    timestamps: {
                                        /** Format: date-time */
                                        created: string;
                                        /** Format: date-time */
                                        completed: string | null;
                                        /** Format: date-time */
                                        updated: string;
                                    };
                                }[];
                                pagination: {
                                    page: number;
                                    limit: number;
                                    total: number;
                                    totalPages: number;
                                };
                            };
                        };
                    };
                };
                /** @description Bad Request - validation failed or invalid input */
                400: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/ledger/transactions/{transactionId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get transaction details
         * @description Full detail for one funding (deposit, withdrawal, refund or internal transfer) owned by the authenticated merchant. Admin credit and debit ids from the transaction list are not fundings and return 404.
         */
        get: {
            parameters: {
                query?: never;
                header?: never;
                path: {
                    transactionId: string;
                };
                cookie?: never;
            };
            requestBody?: never;
            responses: {
                /** @description Transaction details */
                200: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: true;
                            data: {
                                id: string;
                                type: string;
                                status: string;
                                amount: {
                                    usd: number;
                                    crypto: string;
                                    token: string | null;
                                    chain: string | null;
                                    exchangeRate: string;
                                };
                                address: string | null;
                                txnHash: string | null;
                                fee: number;
                                timestamps: {
                                    /** Format: date-time */
                                    created: string;
                                    /** Format: date-time */
                                    completed: string | null;
                                    /** Format: date-time */
                                    updated: string;
                                    /** Format: date-time */
                                    expired: string | null;
                                };
                            };
                        };
                    };
                };
                /** @description Unauthorized - missing or invalid credentials */
                401: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Forbidden - insufficient permissions */
                403: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Not Found */
                404: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Too Many Requests - rate limit exceeded */
                429: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
                /** @description Internal Server Error */
                500: {
                    headers: {
                        [name: string]: unknown;
                    };
                    content: {
                        "application/json": {
                            /** @description Unique request identifier */
                            requestId: string;
                            /** @enum {boolean} */
                            success: false;
                            error: {
                                /** @description Numeric error code (see error-codes.ts) */
                                code: number;
                                /** @description Machine-readable error key */
                                key: string;
                                /** @description Human-readable, for display and logs only - do not parse it. Wording changes without notice; branch on `key` or `code` instead, and read `fields` for per-field detail. On VALIDATION_FAILED each offending field is named: "tradeurl: Trade URL must be ...". */
                                message: string | string[];
                                /** @description Present only on VALIDATION_FAILED. Maps the dotted path of each rejected field to why it was rejected, one entry per field. Capped at 20 entries; `message` names the first 5. */
                                fields?: {
                                    [key: string]: string;
                                };
                                /** @description Structured context for errors that carry it, e.g. ITEM_OVERSTOCKED names the refused items in `details.items`. Absent otherwise. */
                                details?: {
                                    [key: string]: unknown;
                                };
                            };
                        };
                    };
                };
            };
        };
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: never;
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
